{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && rm -rf .vercel/output && mkdir -p .vercel && cp -R frontend/.vercel/output .vercel/output",
  "installCommand": "echo 'skip root install'",
  "framework": null
}
